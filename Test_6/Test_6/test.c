#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <stdlib.h>
#include "Test.h"
int main()
{
	//题目1：实现一个函数，打印乘法口诀表，口诀表的行数和列数自己指定，
	//输入9，输出9 * 9口诀表，输入12，输出12 * 12的乘法口诀表。
//////第一个函数，乘法口诀表Mult_Table(i)：
//	int i = 0;
//	printf("请输入要打印的行数：");
//	scanf("%d", &i);
//	Mult_Table(i);
///////////////////////////////////////////////////////////////////////////
	//题目2：使用函数实现两个数的交换。
    //第二个函数Exchange(i, j)，交换两个数
	//int i = 0;
	//int j = 0;
	//printf("Please input two integer:(i and j)");
	//scanf("%d %d", &i, &j);
	//Exchange(i, j);//这种方法不会交换主函数里面的两个数字的值
///////////////////////////////////////////////////////////////////////////
	//题目3：实现一个函数判断year是不是润年。
	//第三个函数，Leap_Year(year)
	//int i = 0;
	//int year = 0;
	//scanf("%d", &year);
	//i = Leap_Year(year);
	//if (i == 1)
	//{
	//	printf("%d is Leap Year!\n",year);
	//}
	//else
	//{
	//	printf("%d isn't Leap Year!\n",year);
	//}
///////////////////////////////////////////////////////////////////////////
	//题目5：实现一个函数，判断一个数是不是素数。
	//第五个函数，Judge_Prime()
	/*int i = 0;
	int prime = 0;
	printf("Please input a integer :\n");
	scanf("%d", &prime);
	i = Judge_Prime(prime);
	if (i == 1)
	{
		printf("%d is a prime!",prime);
	}
	else
	{
		printf("%d isn't a prime!",prime);
	}*/
	system("pause");
	return 0;
}